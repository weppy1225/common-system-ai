# TT_551 DB 이관 명령어

인수: **$ARGUMENTS**

스크립트 기본 경로: `C:\zinide\workspace_cloud\cloud-wms-doc\output\05 이행(TT)\TT_551`

---

## 실행 규칙

### 인수 없음 또는 `all` → 전체 실행

```powershell
& "C:\zinide\workspace_cloud\cloud-wms-doc\output\05 이행(TT)\TT_551\migrate_all.ps1"
```

### 인수가 특정 버전(`V1` ~ `V10`) → 해당 스크립트만 실행

| 인수 | 실행 스크립트 | 대상 |
|---|---|---|
| `V0` | `migrate_V0_create_db.ps1` | TO DB 데이터베이스 생성 |
| `V1` | `migrate_V1_schema.ps1` | DDL 적용 |
| `V2` | `migrate_V2_code.ps1` | 공통코드 |
| `V3` | `migrate_V3_biz.ps1` | 사업장 |
| `V4` | `migrate_V4_center.ps1` | 센터 |
| `V5` | `migrate_V5_warehouse.ps1` | 창고 |
| `V6` | `migrate_V6_location.ps1` | 위치(로케이션) |
| `V7` | `migrate_V7_config.ps1` | 시스템파라미터 |
| `V8` | `migrate_V8_menu.ps1` | 메뉴 |
| `V9` | `migrate_V9_user.ps1` | 사용자 |
| `V10` | `migrate_V10_functions.ps1` | 함수/프로시저 |

예) 인수가 `V3` 이면:
```powershell
& "C:\zinide\workspace_cloud\cloud-wms-doc\output\05 이행(TT)\TT_551\migrate_V3_biz.ps1"
```

---

## 실행 절차

1. 인수를 확인하여 위 표에서 실행할 스크립트를 결정한다.
2. PowerShell Bash 툴로 해당 스크립트를 실행한다.
3. 실행 결과(STEP 1~3 출력)를 그대로 사용자에게 보여준다.
4. 오류 발생 시 exitcode와 오류 메시지를 출력하고 중단한다.
5. 성공 시 "이관 완료. test/dev 건수를 확인하세요." 를 안내한다.
