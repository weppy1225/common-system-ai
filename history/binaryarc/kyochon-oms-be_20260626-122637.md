---
date: 2026-06-26 12:26:37
author: binaryarc
---

# 작업 히스토리

## ZTEST_SHPD01Mapper 테스트 코드 수정 — ensureShopContFromMdmCont 제거 후 신규 API 적용

### 변경 내용

1. **`ZTEST_SHPD01Mapper.java` 수정**
   - import 추가: `SHPD01ShopCont`
   - `test_insertUpdateProd_rollback` (Order 40):
     - 이전: `mapper.ensureShopContFromMdmCont(prod)` 호출
     - 이후: `mapper.existShopCont()` + `mapper.selectMdmContForShopCont()` 분리 호출
     - 사유: Mapper 테스트에서 CryptoTool 미초기화 환경이므로 암호화 insertShopCont는 제외, SELECT 단계만 검증
   - `test_ensureShopContFromMdmCont_makerContSeqBoundary` (Order 41):
     - 이전: 제거된 `ensureShopContFromMdmCont` 경계값 테스트
     - 이후: `test_existShopCont_selectMdmCont_makerContSeqBoundary`로 교체 (existShopCont + selectMdmContForShopCont 경계값)
     - session.rollback() 불필요 (SELECT only)
   - 사유: 암호화 구현으로 ensureShopContFromMdmCont가 Mapper에서 제거됨
