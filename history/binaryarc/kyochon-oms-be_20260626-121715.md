---
date: 2026-06-26 12:17:15
author: binaryarc
---

# 작업 히스토리

## SHPD01TxComp 단가이력 통합 — insertProdTX/updateProdTX에 BUY/PURCHASE 이력 삽입 추가

### 변경 내용
1. **`SHPD01TxComp.java` 수정**
   - 이전: 단가이력 삽입 없음
   - 이후:
     - `SHPD01PriceHistDao` 필드 추가 (RequiredArgsConstructor 자동 주입)
     - `PRICE_TYPE_BUY = "BUY"`, `PRICE_TYPE_PURCHASE = "PURCHASE"` 상수 추가
     - `insertProdTX`: `insertProd` 후 BUY(판매가)/PURCHASE(입고가) 이력 INSERT (before=null)
     - `updateProdTX`: `selectProd` 결과를 `existingProd`로 캡처 → `updateProd` 후 before=기존단가/after=수정단가 이력 INSERT
     - private `insertPriceHistory(shopProdSeq, beforeSale, afterSale, beforeCost, afterCost, regId)` 헬퍼 추가
   - 사유: shop_prod_price_hist 테이블에 판매가·입고가 변경 이력 적재 요건 반영

### 관련 작업 (동일 세션 병렬 완료)
- `kyochon-oms-be_20260626-121507.md` — 암호화 구현 (ensureShopContFromMdmCont 분리, CryptoTool 적용)
- `kyochon-oms-be_20260626-121436.md` — 단가이력 인프라 (SHPD01PriceHist Bean/Mapper/Dao 신규)
