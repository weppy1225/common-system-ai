---
date: 2026-06-26 13:48:16
author: binaryarc
---

# 작업 히스토리

## shft01c(의탁자 상품 목록) HOT·NEW 뱃지 버그 수정

### 변경 내용

1. **`shft01c.vue` 수정**
   - 이전: HOT 뱃지만 표시, NEW 뱃지 누락 / 두 뱃지 동시 표시 시 겹침
   - 이후:
     - `toProduct`에 `new: product.newYn === 'Y'` 추가
     - template badge-wrap에 `<span v-if="product.new" class="badge-new">NEW</span>` 추가
     - CSS `.badge-new` 스타일 추가 (파란색 #3498db)
     - CSS `.badge-wrap`에 `display: flex; gap: 4px` 추가 → 두 뱃지 나란히 표시
   - 사유:
     - `toProduct`에 `new` 속성 누락 → NEW 뱃지 조건 항상 falsy
     - template에 NEW span 자체 없음
     - `badge-wrap` flex 미설정으로 동시 표시 시 겹침
