---
date: 2026-06-26 12:14:36
author: binaryarc
---

# 작업 히스토리

## SHPD01 단가이력(shop_prod_price_hist) 인프라 파일 신규 생성

### 신규 파일 생성
- `src/main/java/be/sh7000/shpd01/bean/SHPD01PriceHist.java`
  - 쇼핑몰 상품 단가이력 DTO. BaseParam 미상속(단순 INSERT 전용 DTO). @Getter @Setter + Lombok. 필드: prodPriceHistSeq, shopProdSeq, priceTypeCd, beforePrice(BigDecimal), afterPrice(BigDecimal), regId
- `src/main/java/be/sh7000/shpd01/SHPD01PriceHistMapper.xml`
  - MyBatis Mapper XML. insertPriceHist(parameterType=SHPD01PriceHist, useGeneratedKeys=true, keyProperty=prodPriceHistSeq). reg_dt=NOW() 자동 삽입. 시퀀스 방식은 기존 insertProd 패턴(useGeneratedKeys)과 동일하게 적용 — DB에 테이블·시퀀스 미존재 확인 후 PostgreSQL SERIAL/IDENTITY 방식으로 결정
- `src/main/java/be/sh7000/shpd01/SHPD01PriceHistMapper.java`
  - MyBatis Mapper 인터페이스. @Repository, insertPriceHist(SHPD01PriceHist bean) → int
- `src/main/java/be/sh7000/shpd01/SHPD01PriceHistDao.java`
  - DAO 클래스. @Repository @Slf4j @RequiredArgsConstructor(onConstructor=@__(@Autowired)). SHPD01PriceHistMapper 주입. FwPool.DAO_START_LOG/DAO_END_LOG 패턴 적용

### 변경 내용
1. **DB 시퀀스 확인**
   - 이전: 시퀀스명 미확인
   - 이후: DB 쿼리 결과 shop_prod_price_hist 테이블·시퀀스 미존재 확인 → useGeneratedKeys 방식으로 결정
   - 사유: OMS DB 컨벤션 — 추정 금지, 실제 DDL 확인 후 결정. 테이블 DDL 반영 시 PK는 SERIAL/IDENTITY 또는 별도 시퀀스로 생성 예정
