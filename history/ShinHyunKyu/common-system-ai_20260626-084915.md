---
date: 2026-06-26 08:49:15
author: ShinHyunKyu
---

# 작업 히스토리

## STRUCTURE.md 3계층 라벨에 SoT 포인터 추가 → CLAUDE.md §3계층 위임 (SoT 중복 #4, 라이트터치)

### 변경 내용
1. **STRUCTURE.md knowledgebase 트리 직후 — ①②③ 계층 라벨 SoT 포인터 1줄 추가**
   - 이전: STRUCTURE.md 가 `domains/`(② 라벨, line 59)·`_knowledge/`(③ 라벨, line 71)에 ①②③ 계층 라벨만 인라인으로 붙여 사용. 정의·우선순위는 없으나 라벨이 어느 표를 가리키는지 명시 안 됨.
   - 이후: knowledgebase 트리 코드블록 직후에 `> ①②③ 계층 라벨의 정의·우선순위(③>②>①, ③=실제값/①=기본값)는 CLAUDE.md §"시스템(프로젝트)별 분할 — 3계층" 이 SoT` 포인터 1줄 추가.
   - 사유: 재확인 결과 STRUCTURE.md 는 3계층 표를 복제하지 않고 라벨만 사용(드리프트 위험 낮음). 따라서 라벨(유용한 폴더↔계층 신호)은 유지하고, 정의·우선순위 SoT 가 CLAUDE.md 임을 명시하는 라이트터치만 적용.

### 비고
- CLAUDE.md 무수정: §"시스템(프로젝트)별 분할 — 3계층" 표가 SoT 정본으로 이미 단독.
- STRUCTURE.md 59·71행 ②/③ 라벨 유지(제거 안 함) — 폴더↔계층 매핑 신호로서 유용.
- #4 는 #1~#3 대비 경미(표 복제 없음). 라이트터치(포인터 1줄)로 종결.
