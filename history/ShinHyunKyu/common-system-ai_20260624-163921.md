---
date: 2026-06-24 16:39:21
author: ShinHyunKyu
---

# 작업 히스토리

## 깨진 api-list.md 수리 + 더미 DB 변경이력 파일 삭제

### 변경 내용
1. **`spec/common-system/_knowledge/interface/api-list.md` 수리 (#1)**
   - 이전: 헤딩 블록 중복 병합(`# ERP 연동 API 목록`/`## 1`/`### 1.1` 뒤에 `# API 목록`/`## 2`/`### 2.1` 재등장), detail 링크 16개 중 15개가 경로 오류(`E2W_*.md`)+파일 부재
   - 이후: 중복 헤딩 제거 + 번호 정리(2.x→1.x), 실재하는 `e2w-prod-reg.md`만 링크 유지, 나머지 코드는 링크 해제(코드표기) + "링크 걸린 것만 상세 작성됨, 작성 시 detail/{코드}.md 추가 후 링크" 안내. version 1.0.0→1.0.1
   - 사유: 깨진 병합·dangling 링크 16개 제거. 실제 detail 문서 작성은 별도 작업

2. **`patterns/20-database/01-change-history.md` 삭제 (#3)**
   - 이전: 더미 데이터(테스트 테이블 `mdm_test`, `prod_id` 같은 날 추가→삭제)만 담긴 수기 변경이력
   - 이후: `git rm`
   - 사유: 실 스키마 변경이력 SoT는 실DB(`/SD_db_apply` 반영분·실 DDL)다. 더미뿐이고 인바운드 참조 0건 확인

### 비고 (배치 보고 13건 중 나머지 처리 방침)
- 이미 해결(stale): #4 `30-backend/00-overview`(이번 세션 3계층 정의 채움)
- 건드리지 않음(사람 전용): #13 `mdpr01-00-domain.md`(CLAUDE.md 자동화 금지)
- 의도된 스캐폴드·진입점 유지: #8 `30-src-index/README`(정적 색인 미보유 선언), #9·#10 00-overview 진입점, #11·#12 OMS 미착수 스캐폴드
- 콘텐츠 보강 필요(중복정리 아님): #2 `patterns/00-overview`, #5 `20-pda/00-overview`, #6 `glossary-std`, #7 `10-domain/README`
