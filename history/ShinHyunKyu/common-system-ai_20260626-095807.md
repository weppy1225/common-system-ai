---
date: 2026-06-26 09:58:07
author: ShinHyunKyu
---

# 작업 히스토리

## SoT 중복 Medium/Low(#10~#16) 검증 및 잔여 3건 정리

### 검증 결과 요약
감사 보고서의 #10~#16 중 대부분은 과장/오인으로 확인. 실제 조치는 #10·#12·#13 세 건의 라이트 포인터만.
- #11 prototype 파일역할 ↔ STRUCTURE §prototype: 행위규칙 vs 구조로 상보적 → 비이슈(무조치).
- #14 Bean Validation: coding-convention §7 이 규칙 정본, header-detail §9·controller-rules 는 코드예시 사용 → 비이슈(무조치).
- #15 FE 폴더 ↔ 00-overview §6: §6 은 patterns 문서 트리이지 views/be 소스 구조 아님 → 오인(무조치).
- #16 sql-query-style §1 ↔ §13: §13 은 "종합 스타일 요약표"(정렬·콤마 포함 전체 요약, 이미 라벨) → 비이슈(무조치).

### 변경 내용
1. **#10 git 신원 도출 SoT 포인터 추가** (SoT = `SET_USER_USAGE.md`)
   - `CLAUDE.md` 작업 히스토리 `{작업자}` 규칙: 끝에 `(신원 3종 의미·도출법 SoT → SET_USER_USAGE.md)` 추가.
   - `.claude/rules/md-frontmatter.md` `author` 도출 규칙: `신원 3종 의미·도출법 SoT → SET_USER_USAGE.md` 추가.
   - 사유: 두 곳은 운영 맥락상 git config 도출 규칙을 자체 진술(self-contained)하되, 신원 3종 정본을 SET_USER_USAGE 로 명시 바인딩.

2. **#12 header-detail §5 — 기본 Comp 골격 포인터 추가**
   - §5 서두에 `기본 try-catch 골격 → 06-comp-writing-rules.md §3, 아래는 헤더+상세 고유 로직 적용 예` 1줄 추가.
   - 사유: header-detail §5 예제가 06-comp 의 표준 try-catch 골격을 반복하므로, 골격 SoT 를 명시하고 고유 로직만 강조.

3. **#13 header-detail §7 — §11(채번 정본)로 위임**
   - §7 Dao 채번 예제에서 `DocNoBean` 빌더 전체 코드를 제거하고 `빌더 상세 → §11` 포인터로 축약(§7 은 Dao 가 채번을 insert 전에 감싸는 위치만 표시).
   - 사유: §7·§11 이 동일 파일 내 `DocNoBean.docNoPubBuilder()` 코드를 중복. §11 "문서번호 채번 패턴"을 정본으로 두고 §7 은 위치만. (dao-writing-rules·menu-code-rule 의 채번 중복은 감사 주장과 달리 없음 — dao-rules 엔 부재, menu-code §0 은 단순 경고.)

### 비고
- 검증: `check-doc-refs.py` 깨진 참조 10건(기존 baseline 동일, 증가 0), 이번 수정 파일 깨진 참조 없음. PYEXIT=1 은 기존 OMS spec-상대경로 오판 건으로 무관.
