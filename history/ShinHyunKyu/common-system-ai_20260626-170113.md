---
date: 2026-06-26 17:01:13
author: ShinHyunKyu
---

# 작업 히스토리

## MD 전수 감사 후속 정리: 리브랜딩·깨진참조·SoT중복·스킬동작 일괄 수정

전체 MD(245건) 4축 감사(리브랜딩/깨진참조/SoT중복/스킬동작) 결과를 순서대로 반영했다. `history/` 로그는 정정 대상에서 제외.

### 1. 리브랜딩 — `ai-kb` 폐기 별칭 4건 → `common-system-ai`

1. **CLAUDE.md L84** — `허브(ai-kb)` → `허브(common-system-ai)`
2. **STRUCTURE.md L129** — `허브(ai-kb)` → `허브(common-system-ai)`
3. **.claude/rules/system-detect.md L2(frontmatter)** — `허브(ai-kb=common-system-ai)` → `허브(common-system-ai)`
4. **.claude/rules/system-detect.md L20(표)** — `허브(ai-kb)` → `허브(common-system-ai)`
   - 사유: `doc 레포→AI 허브` 용어 통일 때 누락된 옛 별칭 잔재 제거.

### 2. 깨진 참조 1건

1. **.claude/rules/db-convention.md §4**
   - 이전: 공통코드 확인을 `01-common-code.md`(실파일 없음)로 지시
   - 이후: `spec/{프로젝트}/_knowledge/db-schema/90-common-code.md`(실파일)로 정정

### 3. SoT 중복 2건

1. **API URL 규칙 위임** — `patterns/30-backend/20-rule/02-menu-code-rule.md`
   - 이전: §2가 패키지 경로 + API URL 도출 규칙 + `@RequestMapping URL` 매핑표를 독립 재정의(01과 중복)
   - 이후: §2를 "Java 패키지 도출"로 한정. URL 베이스·메서드·인터페이스 ID 명세는 `01-api-naming-rule.md`로 위임(SoT 명시). 매핑표는 메뉴코드→패키지만 유지. frontmatter description·tags(`api-url`)도 정리
2. **prototype 트리·파일역할표 이관** — `STRUCTURE.md` ↔ `CLAUDE.md`
   - 이전: 상세 트리 + 파일역할표가 CLAUDE.md에 있고 STRUCTURE.md는 요약만(역할 역전, CLAUDE.md L59 자체 규칙과 모순)
   - 이후: 상세 트리 + 파일역할표를 STRUCTURE.md §prototype로 이관, CLAUDE.md는 요약 + 포인터만 유지

### 4. 스킬 동작 정정

1. **미정의 서브에이전트 생성** (PI_be_all/comp/dao/inven/mapper가 호출)
   - 신규: `.claude/agents/code-layer-explorer.md`, `.claude/agents/db-doc-reader.md` (읽기 전용 탐색 에이전트)
2. **`/util-work-output`(미존재) 제거** — PI_be_all Phase10, PI_be_comp Step10 → 실존 `/PI_fe_all` 안내로 교체
3. **`02-fe-code-rule.md`(미존재)** — PI_fe_all → `patterns/40-frontend/20-convention/01-naming.md`·`03-backend-spec-consumption.md`로 교체
4. **`01-router-menu-register.md` 폴백** (WMS에 없음) — PI_fe_all·PI_fe_list → 프로젝트 고유 맵(있을 때) 또는 공통 `patterns/40-frontend/10-architecture/03-menu-registration.md` 참조로 보강
5. **사라진 크로스스킬 참조 제거** — SD_331의 `/SD_212`·`/SD_212_DDL`, SD_333의 `/SD_211` → 실존 스킬(`/SD_333` 등)로 정리/삭제
6. **구버전 모델 지정 갱신** — SD_api·SD_db·PI_be_inven `model: claude-opus-4-7` → `claude-opus-4-8`

### 신규 파일 생성
- `.claude/agents/code-layer-explorer.md`
  - 메뉴코드의 기존 BE 레이어 파일 현황을 형제 BE 레포에서 조사·보고(읽기 전용)
- `.claude/agents/db-doc-reader.md`
  - 테이블명의 컬럼·PK/FK를 프로젝트 DB 스키마 문서에서 조회·보고(읽기 전용)

### 미결(사용자 결정 필요)
- `.claude/skills/deploy/SKILL.md` — FTP 호스트/계정/비밀번호 평문 하드코딩(L60~66, 137~138, 152). 이미 git 히스토리에 노출. SET_ENV 치환 전환 + 자격증명 재발급 여부는 사용자 결정 대기(임의 변경 시 배포 동작 영향 우려로 미수정).
