---
date: 2026-06-26 14:50:19
author: ShinHyunKyu
---

# 작업 히스토리

## 빈 스캐폴드 30-src-index 삭제 (도출규칙·SIF예외 모두 타 문서 중복)

### 변경 내용
1. **`knowledgebase/30-src-index/` 폴더 삭제 (git rm -r, README.md 1건)**
   - 사유: ① 메뉴코드→소스경로 도출 규칙은 이미 `patterns/40-frontend/10-architecture/01-folder-structure.md`·`00-overview.md`·`20-convention/01-naming.md` 및 다수 BE/FE 스킬에 존재. ② "규칙 외 수동 노트 보관처" 역할은 생성 이후 노트 0건이며, 대표 예외(SIF 위치)는 `wms-sif-convention.md`·`domains/wms/interface-convention/` 에 이미 있음. 고유 가치 없는 빈 스캐폴드.

2. **참조 정리 (4개 라이브 파일)**
   - `STRUCTURE.md` — knowledgebase 트리에서 항목 제거 + "정본은 소스" 줄의 `(위치는 30-src-index)` 를 `patterns/40-frontend/ 도출 규칙` 안내로 교체
   - `knowledgebase/00-overview.md` — 읽는 순서·경계규칙 두 줄에서 `30-src-index`(및 이미 삭제된 `20-md-index`) 참조 제거, 소스 위치는 도출 규칙/직접 스캔으로 안내
   - `scripts/gen-md-map.py` — `FOLDER_ROLE` 에서 `knowledgebase/30-src-index` 라벨 항목 제거

### 검증
- `python scripts/gen-md-map.py` 재생성 → `edu/md-index.html` (md 194개, 삭제 반영).
- `python scripts/check-doc-refs.py` → 깨진 참조 0, 미참조 패턴 0, BOM 0 (종료코드 0).
- 라이브 파일 내 `30-src-index` 잔여 참조 0 (history/ 과거 기록 제외).
