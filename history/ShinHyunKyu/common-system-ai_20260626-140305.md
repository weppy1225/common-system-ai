---
date: 2026-06-26 14:03:05
author: ShinHyunKyu
---

# 작업 히스토리

## MD 구조 정리 — BOM 일괄 제거 + 재발 방지책 + 로컬 잔여물 정리

### 변경 내용
1. **UTF-8 BOM 일괄 제거 (.md 26개)**
   - 이전: `.claude/rules/`(common-code·oms-backend/db/frontend-convention·system-detect 5개), `spec/kyochon-oms/` 18개, `knowledgebase/domains/oms/README.md`, `patterns/_common-arch/common-code.md` 등 26개 파일이 파일 앞에 BOM(`EF BB BF`) 포함
   - 이후: BOM 제거 (내용 변화 없음, 첫 3바이트만 제거)
   - 사유: BOM이 frontmatter 첫 줄 `---` 파싱을 방해 → rule 조건부 로딩이 깨질 위험. 실제로 frontmatter 검출 오탐을 유발하고 있었음.

2. **STRUCTURE.md BOM 제거**
   - 이전: 1번 줄 `﻿---` (BOM 포함)
   - 이후: `---`
   - 사유: 위와 동일 (에디터가 저장 중 BOM 탈락된 것을 정리·확정)

3. **재발 방지책 — md-frontmatter.md 규칙 추가**
   - 「기본 원칙」에 "UTF-8 BOM 금지" 항목 추가. 방지(.editorconfig)·검출(check-doc-refs.py) 경로 명시.

### 신규 파일 생성
- `.editorconfig`
  - `charset = utf-8`(BOM 없는 UTF-8)로 에디터가 BOM을 붙이지 못하게 1차 차단. LF·최종개행·trailing whitespace 정리 포함(.md는 trim 예외, .bat/.ps1은 CRLF).
- `scripts/check-doc-refs.py` 에 `check_bom()` 추가
  - 전 `.md` 파일 BOM 검출 → 발견 시 ERROR 출력·종료코드 1 (커밋/CI 게이트). 기존 참조 무결성 검사와 통합.

### 기타 (이전 단계에서 수행, 본 작업과 함께 정리)
- `deliverables/30-output/04 구현(PI)/node_modules` (15MB, git 미추적·gitignore) 로컬 삭제 — git 영향 없음.
- `deliverables/20-sources/SD_312/` 하위 PNG 276개 삭제 (사용자 직접 수행, git 추적 파일 → 삭제로 반영됨).

### 검증
- `python scripts/check-doc-refs.py` → 깨진 참조 0 · 미참조 패턴 0 · **BOM 0** · 종료코드 0
